server:
  port: 0
  
  
spring:
#  application:
#    name: stock-ws
    
  datasource:
    driver-class-name: org.h2.Driver
    password: ''
    url: jdbc:h2:mem:testDB
    username: sa
  h2:
    console:
      enabled: true
      path: /h2-console/
      settings:
        web-allow-others: true

